// Pattern Practice: Specification Pattern
// Try this specification pattern prompt with Copilot:

// Create a user search component that:
// - Accepts search query input with debounced typing
// - Displays search results in a scrollable list
// - Shows loading spinner during search requests
// - Handles empty states with helpful messaging
// - Implements keyboard navigation (arrow keys, enter)
// - Includes accessibility attributes for screen readers
// - Filters out inactive users from results
// - Limits results to 50 items with "show more" option

import React from 'react';

// Place your cursor below and try the specification pattern: